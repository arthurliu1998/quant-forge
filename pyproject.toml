[project]
name = "trade-forge"
version = "0.1.0"
description = "Multi-agent trading analysis system"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.21"]
